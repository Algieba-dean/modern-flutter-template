# Copy values from this file into your shell, CI variables, or deployment
# environment. Do not commit real credentials or environment-specific secrets.

APP_ENV=development
API_BASE_URL=https://api.example.com
